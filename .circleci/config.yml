version: 2
jobs:
  build:
    machine: true
    steps:
      - run:
          command: |
            sudo pip install pipenv
            pipenv install
            pipenv run "python manage.py test"