// I chose to represent our relationships between cards here as the board module was getting quite large and bloated. 

//You can have either a relationship or a subclass, not both. 



const inititalState = {
  relationships : {

  }
}

export default (state = inititalState, action) => {
  switch(action.type) {


    default:
      return state
  }
}